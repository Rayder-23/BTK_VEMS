@{
    ViewData["Title"] = "Change Theme";
}

<div class="portal-card">
    <h1 class="h5 fw-bold mb-1">@(ViewData["Title"]?.ToString() ?? "General Settings")</h1>
    <p class="text-muted mb-4">Choose how the teacher portal looks on this device. Your preference is saved in your browser.</p>

    <div class="d-flex flex-column gap-2 teacher-theme-options" style="max-width: 320px;">
        <label class="teacher-theme-option">
            <input type="radio" name="teacherTheme" value="default" />
            <span class="teacher-theme-option-body">
                <i class="bi bi-sun"></i>
                <span>
                    <span class="fw-semibold d-block">Default</span>
                    <small class="text-muted">Light workspace with purple accents</small>
                </span>
            </span>
        </label>
        <label class="teacher-theme-option">
            <input type="radio" name="teacherTheme" value="dark" />
            <span class="teacher-theme-option-body">
                <i class="bi bi-moon-stars"></i>
                <span>
                    <span class="fw-semibold d-block">Dark</span>
                    <small class="text-muted">Reduced glare for low-light use</small>
                </span>
            </span>
        </label>
    </div>
</div>

@section Scripts {
    <script>
        (function () {
            var key = 'vems-teacher-theme';
            var options = document.querySelectorAll('input[name="teacherTheme"]');
            var saved = localStorage.getItem(key) || 'default';

            options.forEach(function (input) {
                input.checked = input.value === saved;
                input.addEventListener('change', function () {
                    if (!input.checked) return;
                    localStorage.setItem(key, input.value);
                    document.body.dataset.teacherTheme = input.value === 'default' ? '' : input.value;
                });
            });
        })();
    </script>
}
